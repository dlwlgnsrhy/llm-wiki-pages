# Curated AI-native brand wiki

This public repository contains only the generated static artifact for
`https://dlwlgnsrhy.github.io/llm-wiki-pages/`.

- The private Wiki remains the canonical source.
- `public/` is generated from an explicit four-document allowlist.
- Public feedback links open the private `dlwlgnsrhy/llm-wiki` Issue form and
  therefore require an authorized GitHub login.
- Do not add private Wiki, raw research, customer data, or operating notes here.

The deployment workflow replaces `__PUBLIC_DEPLOY_SHA__` with the public
repository commit SHA before uploading the Pages artifact.
